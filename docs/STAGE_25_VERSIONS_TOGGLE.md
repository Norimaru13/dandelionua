# Етап 25. Згортання таблиці і колір тексту в ній

Дата: 19.08.2026

---

## 1. Що змінено

- Текст у таблиці `#28402f`.
- Заливку й лінії таблиці не чіпав — лишив як я вже поставив у файлі.
- Комірка «Підтримувані версії:» / «Supported versions:» стала кнопкою.
- Таблиця завжди стартує згорнутою. Клік по цьому рядку розгортає й згортає решту.

---

## 2. Як саме

Кнопка `data-versions-toggle` у першому рядку. Клас `is-open` на `table.versions`. Без нього сховані рядок колонок і `tbody`.

Логіка в `js/main.js`.

---

## 3. Що варто знати

Згорнутий стан — лише одна смужка з написом у тій самій рамці.

---

## 4. Обмеження

Немає окремої іконки стрілки — я її не давав.

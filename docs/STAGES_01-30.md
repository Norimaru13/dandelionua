# Етапи 01–30

Дата збору: 19.08.2026

Окремі файли STAGE_01 … STAGE_30 зібрані сюди, щоб було зручніше читати.
Наступний такий збір — етапи 31–60.

Зміст:

1. База сайту
2. Максимально проста версія
3. Папка документації і іконка вкладки
4. Хедер
5. Більший хедер, смужки, мова
6. Обрізання пухнастої кульбаби
7. Закруглення меню і вибір мови
8. Текст на сторінці, Tiny5, закруглення знизу
9. Виділення тексту і три стани кульбаби
10. Нові текстури кнопки і меню зліва
11. Меню з кнопки і іконка мови
12. Іконки соцмереж, перегляди і вподобайки
13. GitHub Pages
14. Картинки на Pages і Supabase
15. Ключі Supabase і живі текстури
16. Чотири стани кнопок, сховане меню, без тестової сторінки
17. Порядок іконок і посилання Modrinth
18. Усі іконки в `assets/icons/`
19. Текст і таблиця версій на головній
20. Оформлення таблиці і відступи body
21. Кольори рамки, Forge 1.19.4, іконка CurseForge
22. Заливка таблиці, фіксований хедер, скролбар
23. Інверсія заливки і колір тексту
24. Зелена палітра тексту і таблиці
25. Згортання таблиці і колір тексту в ній
26. Сайдбари, ширший другий абзац, таблиця
27. Скролбар, resource packs, мова, футер
28. Великий скролбар і звук кліку
29. Власна форма скролбара
30. Свій прямокутний скролбар без фону

---

# Етап 01. База сайту

Дата: 19.08.2026

Це перша робоча версія сайту Dandelion_ua. Її можна відкрити локально і вже клікати по сторінках.

---

## 1. Що додано

- Головна сторінка
- Список модів у вигляді карток
- Окрема сторінка мода (опис, скріншоти, вимоги, кнопка завантаження)
- Базова навігація: Головна / Моди
- Перемикач мови EN / UA (англійська стоїть за замовчуванням)
- Адаптивність для телефона і комп'ютера
- Три демо-моди, щоб сітка і сторінки не були порожніми

---

## 2. Як це зроблено

Стек навмисно простий: чистий HTML + CSS + JavaScript. Без фреймворків.

### Файли

```
dandelionua/
  index.html              головна
  mods.html               список модів
  mod.html                один мод (?id=...)
  css/style.css           усі стилі
  js/mods-data.js         дані модів (єдине місце)
  js/i18n.js              переклади інтерфейсу і мова
  js/main.js              картки, пошук, галерея, шапка
  assets/logo.svg
  assets/favicon.svg
  assets/placeholder-*.svg
  Інструкція.txt
  STAGE_01_BASE.md
```

### Дизайн

- Темний фон, сітка 16px, товсті рамки, тінь «як у блоку».
- Акценти: жовтий `#f4c430`, синій `#4da3ff`.
- Додаткові: темно-сірий і зелений `#6bbf4e`.
- Шрифт: **DotGothic16** з Google Fonts для всього тексту.
- Запасний шрифт: **Pixelify Sans**. DotGothic16 погано тягне кирилицю, тому українські літери беруться з Pixelify Sans. Це тимчасове рішення, поки не підберемо один піксельний шрифт з повною підтримкою EN + UA.

### Мова

- Ключ у `localStorage`: `dandelion-lang` (`en` або `ua`).
- Тексти інтерфейсу лежать у `js/i18n.js`.
- Тексти модів лежать у `js/mods-data.js` у парі `en` / `ua`.
- На HTML-елементах стоїть `data-i18n="ключ"`.
- Мова застосовується одразу після завантаження і після кліку EN / UA.

### Моди

Один запис у `window.Dandelion.mods` дає картку, пошук і сторінку мода.

Важливі поля:

- `id` — для посилання `mod.html?id=...`
- `featured` — чи показувати на головній
- `title`, `short`, `description` — об'єкти `{ en, ua }`
- `version`, `mcVersion`, `loader`
- `downloadUrl` — порожній рядок = кнопка «Незабаром»
- `requirements` — списки для обох мов
- `screenshots` — шляхи до картинок
- `status: "placeholder"` — жовтий бейдж «Демо» і синє попередження на сторінці мода

Зараз у базі три вигадані моди:

1. `wild-dandelions`
2. `pocket-workshop`
3. `skyroot-trails`

Це не справжні моди друга. Вони лише тримають макет.

### Сторінки

- `index.html` — герой, обрані моди, блок про автора.
- `mods.html` — усі картки + пошук по назві, опису, лоадеру і версії.
- `mod.html` — збирається з даних. Є галерея (клік по мініатюрі міняє велике фото) і бічна колонка з вимогами. Якщо `id` невідомий, показується «мод не знайдено».

Сайт можна відкрити просто файлом (`index.html`) або через локальний сервер. Запити на сервер не потрібні.

---

## 3. Що варто знати перед наступними змінами

1. Новий мод додаю тільки в `js/mods-data.js`. HTML чіпати не треба.
2. Новий рядок інтерфейсу додаю в обидві мови в `js/i18n.js`, потім ставлю `data-i18n` у HTML.
3. Справжні скріншоти кладу в `assets/` і прописую шлях у записі мода.
4. Коли з'явиться файл мода — вписую URL у `downloadUrl` і прибираю `status: "placeholder"`.
5. Кольори і шрифт міняю в `:root` у `css/style.css`.
6. Шапка зараз скопійована в три HTML-файли. Якщо міняю навігацію — міняю в усіх трьох.
7. Перед будь-якою зміною вимог спочатку оновлюю `Інструкція.txt`.

---

## 4. Обмеження цього етапу

- Немає справжніх модів, скріншотів і файлів для завантаження.
- Немає бекенду, акаунтів, коментарів і статистики завантажень.
- Пошук працює тільки на сторінці списку і тільки в браузері.
- Кирилиця йде через запасний шрифт, не через DotGothic16.
- На дуже вузькому екрані меню ховається за кнопкою ☰.

Цього досить, щоб далі додавати функції по одній і не ламати вже зібрану базу.

---

# Етап 02. Максимально проста версія

Дата: 19.08.2026

Прибрав усе зайве з етапу 01. Зараз на сайті лише те, що я явно залишив.

---

## 1. Що змінено

- Прибрано головний екран з вигаданими текстами
- Прибрано список модів, сторінку мода, пошук, картки
- Прибрано демо-моди і заглушки-картинки
- Прибрано оформлення (темна тема, сітка, кнопки, шрифти)
- Лишилось: нік **Dandelion_ua**, кульбаба, перемикач EN / UA
- Оновлено `Інструкція.txt`: нічого не додавати від себе; тексти, які я даю українською, перекладати і додавати англійською; дизайн поки не робити

---

## 2. Як це зроблено

### Файли зараз

```
dandelionua/
  index.html
  css/style.css
  js/i18n.js
  assets/logo.svg
  assets/favicon.svg
  Інструкція.txt
  STAGE_01_BASE.md
  STAGE_02_SIMPLE.md
```

Видалено:

- `mods.html`
- `mod.html`
- `js/main.js`
- `js/mods-data.js`
- `assets/placeholder-meadow.svg`
- `assets/placeholder-trails.svg`
- `assets/placeholder-workshop.svg`

### Сторінка

У `index.html` є тільки:

- іконка-кульбаба (`assets/logo.svg`)
- текст `Dandelion_ua`
- кнопки `EN` і `UA`

### Мова

Уся логіка в `js/i18n.js`.

- Ключ у `localStorage`: `dandelion-lang` (`en` або `ua`)
- За замовчуванням англійська
- Об'єкт `translations` зараз порожній: текстів від мене ще немає
- Коли з'явиться текст, він іде в `en` і `ua`, а в HTML ставиться `data-i18n="ключ"`
- Активна мова підсвічується жирним на кнопці

### Стилі

У `css/style.css` лише розмір кульбаби (32px) і жирний стан активної мови. Іншого оформлення немає.

---

## 3. Що варто знати перед наступними змінами

1. Новий текст з'являється тільки якщо я його дав. Українською — оригінал, англійською — переклад.
2. Картинки, скріншоти і описи модів не вигадувати.
3. Дизайн не чіпати, поки я сам не скажу.
4. Кульбаба тимчасова, пізніше замінимо.
5. Старі сторінки модів не повертати «про запас».

---

## 4. Обмеження

- Перемикач мови працює і пам'ятає вибір, але перекладати поки нічого: контенту немає.
- Сайт — одна сторінка.

---

# Етап 03. Папка документації і іконка вкладки

Дата: 19.08.2026

---

## 1. Що змінено

- Інструкція і всі файли стадій перенесені в окрему папку `docs/`
- Іконку вкладки браузера замінено на `assets/dandelion_ua_site_logo.png`
- Кульбаба на самій сторінці не чіпалась
- Старий `assets/favicon.svg` прибрано: вкладка більше його не використовує

---

## 2. Як це зроблено

### Документація

Тепер так:

```
dandelionua/
  docs/
    Інструкція.txt
    STAGE_01_BASE.md
    STAGE_02_SIMPLE.md
    STAGE_03_DOCS_AND_FAVICON.md
  index.html
  css/
  js/
  assets/
```

У `Інструкція.txt` записано, що нові етапи треба класти саме в `docs/`.

### Іконка вкладки

У `index.html` замість:

```html
<link rel="icon" href="assets/favicon.svg" type="image/svg+xml">
```

стоїть:

```html
<link rel="icon" href="assets/dandelion_ua_site_logo.png" type="image/png">
```

Файл PNG я сам поклав у `assets/`. На сторінці як і раніше показується `assets/logo.svg`.

---

## 3. Що варто знати перед наступними змінами

1. Нові `STAGE_*.md` і правки інструкції — тільки в `docs/`.
2. Іконка вкладки і картинка на сторінці — це різні файли.
3. Текст на сторінці в цьому етапі не мінявся.

---

## 4. Обмеження

- Деякі браузери кешують іконку вкладки. Якщо стара кульбаба ще висить — треба оновити сторінку з очищенням кешу.

---

# Етап 04. Хедер

Дата: 19.08.2026

---

## 1. Що змінено

- Зібрано хедер кольору `#6371af`
- Фон сторінки став `#67a77a`
- Між хедером і тілом — дві тонкі смужки: зверху `#0057b8`, знизу `#ffd700`
- Нік прибрано
- Зліва в хедері кнопка з `assets/dandelion.png` у тонкому квадраті з закругленими краями
- При наведенні на кнопку показується `assets/dandelion_puff.png`
- Клік по кнопці відкриває / ховає порожнє меню зліва
- По центру хедера стоїть `assets/dandelion_ua_header.png`
- Перемикач EN / UA лишився справа в хедері
- Іконка вкладки не мінялась

---

## 2. Як це зроблено

### Файли

- `index.html` — розмітка хедера, смужок і порожнього меню
- `css/style.css` — кольори, смужки, квадрат кнопки, центр картинки
- `js/main.js` — відкриття / закриття меню
- `js/i18n.js` — без змін, мова як і раніше

### Кнопка-кульбаба

Кнопка `.menu-btn`: 48×48, рамка 1px, `border-radius: 8px`.

Усередині два зображення. За замовчуванням видно `dandelion.png`, при `:hover` — `dandelion_puff.png`.

У PNG багато порожнього поля навколо квітки, тому картинка трохи збільшена (`transform: scale(1.8)`), щоб у квадраті було видно саму кульбабу.

### Меню

`.side-menu` стоїть зліва під хедером, порожнє. Показ через зняття атрибута `hidden`. Повторний клік ховає.

### Центр хедера

`.header-title` вирівняно абсолютно по центру хедера, щоб ліва кнопка і права мова його не зсували.

---

## 3. Що варто знати перед наступними змінами

1. Пункти в меню ще не додавати, поки я їх не дам.
2. Кольори хедера, фону і смужок не міняти без моєї вказівки.
3. `assets/logo.svg` більше не використовується на сторінці.
4. Іконка вкладки як і раніше `assets/dandelion_ua_site_logo.png`.

---

## 4. Обмеження

- Меню порожнє: у ньому немає пунктів і тексту.
- Закривається тільки повторним кліком по кульбабі.

---

# Етап 05. Більший хедер, смужки, мова

Дата: 19.08.2026

---

## 1. Що змінено

- Хедер став удвічі вищий: `min-height` 64px → 128px, вертикальні відступи 8px → 16px
- Смужки `#0057b8` і `#ffd700` стали втричі товщі: 2px → 6px кожна
- Зверху додано третю смужку `#96a5d5`, у 4 рази тоншу за інші: 1.5px
- Квадрат кульбаби більший за саму квітку (зазор навколо), колір `#96a5d5`, рамка 2.5px замість 1px
- Розмір самої кульбаби не змінювався (як і раніше 48px усередині)
- Кнопка лишилась зліва і стоїть по вертикалі по центру хедера
- `dandelion_ua_header` збільшено до 96px заввишки, зверху і знизу лишається відступ у хедері
- Мова: одна кнопка — чорне коло. Під нею прямокутник з закругленими кутами, у ньому EN / UA
- Ліве меню і меню мови з’являються з плавною анімацією

---

## 2. Як це зроблено

### Розміри хедера

`.site-header`: `min-height: 128px`, `padding: 16px 12px`. Горизонтальний відступ 12px не чіпав, щоб кульбаба не з’їхала вбік.

`.header-title`: `height: 96px` у хедері 128px, тож зверху і знизу лишається поле.

### Смужки

`.header-stripes` висота 13.5px:

1. `#96a5d5` — 1.5px
2. `#0057b8` — 6px
3. `#ffd700` — 6px

### Кнопка-кульбаба

Зовнішній квадрат `.menu-btn` 68×68, фон і рамка `#96a5d5`, рамка `2.5px`.

Усередині `.menu-btn-icon` як і раніше 48×48 з тим самим `scale(1.8)`, тож квітка візуально така сама.

### Мова

`.lang-toggle` — чорне коло `.lang-dot` (іконку замінимо, коли дам файл).

`.lang-menu` — прямокутник, `border-radius: 8px`, фон як у лівого меню `#6371af`. Відкривається під кнопкою.

Логіка відкриття в `js/main.js`. Вибір EN / UA як і раніше в `js/i18n.js`.

### Анімація

`.side-menu` і `.lang-menu` ховаються через `opacity` + `visibility` + невеликий зсув. Клас `.is-open` показує їх за 0.25s. Атрибут `hidden` прибрав, бо через нього анімація не грає.

---

## 3. Що варто знати перед наступними змінами

1. Іконку мови ставити замість `.lang-dot`, не чіпаючи саму логіку кнопки.
2. Пункти лівого меню все ще не додавати, поки я їх не дам.
3. Товщина смужок рахується від етапу 04: було 2px, стало 6px; верхня нова — 6 / 4 = 1.5px.

---

## 4. Обмеження

- Коло мови тимчасове, без іконки.
- Ліве меню порожнє.

---

# Етап 06. Обрізання пухнастої кульбаби

Дата: 19.08.2026

---

## 1. Що змінено

- При наведенні на кнопку `dandelion_puff` більше не обрізається зверху

---

## 2. Як це зроблено

`dandelion_puff.png` вища за `dandelion.png`: непрозора частина починається вище (y=160 проти y=224).

Іконка стоїть у квадраті 48×48 і збільшена через `scale(1.8)`. На `.menu-btn-icon` стояло `overflow: hidden`, тому верх пухнастої кульбаби зрізався.

Поставив `overflow: visible`. Квітка того ж розміру, зайві пікселі зверху просто заходять у поле квадрата `#96a5d5` — там якраз є запас.

---

## 3. Що варто знати перед наступними змінами

1. Обрізати іконку через `overflow: hidden` на `.menu-btn-icon` не треба: пухнаста вища.
2. Розмір кульбаби не чіпав.

---

## 4. Обмеження

Немає.

---

# Етап 07. Закруглення меню і вибір мови

Дата: 19.08.2026

---

## 1. Що змінено

- У лівого меню кульбаби з’явилось закруглення
- Вибір мови більше не зливається з хедером: окрема панель і кнопки EN / UA
- На сайті стоїть піксельний шрифт

---

## 2. Як це зроблено

### Меню кульбаби

`.side-menu` має `border-radius: 0 12px 12px 0` — закруглені правий верх і правий низ, лівий край лишається рівним до краю екрана.

### Вибір мови

Панель `.lang-menu` того ж кольору, що квадрат кульбаби: фон і рамка `#96a5d5`, рамка 2.5px, `border-radius: 8px`.

Кнопки EN / UA:

- звичайний стан — прозорі, чорний текст
- наведення — фон `#6371af`
- активна мова — фон `#0057b8`, текст `#ffd700`

Інших підписів не додавав.

### Шрифт

Підключено Google Fonts у `index.html`:

- **DotGothic16** — основний
- **Pixelify Sans** — запасний, бо в DotGothic16 майже немає кирилиці

`body` і `button` використовують цей стек.

---

## 3. Що варто знати перед наступними змінами

1. Іконку мови як і раніше ставити замість `.lang-dot`.
2. Кольори панелі мови взяті з уже заданих: `#96a5d5`, `#6371af`, `#0057b8`, `#ffd700`.
3. Пункти лівого меню все ще не додавати, поки я їх не дам.

---

## 4. Обмеження

- Коло мови тимчасове.
- Кирилиця йде через Pixelify Sans.

---

# Етап 08. Текст на сторінці, Tiny5, закруглення знизу

Дата: 19.08.2026

---

## 1. Що змінено

- У меню кульбаби закруглення лишилось тільки знизу, зверху прибрано
- Посередині тіла сторінки стоїть текст, який я дав
- Шрифт змінено на **Tiny5** з Google Fonts

---

## 2. Як це зроблено

### Меню

`.side-menu` тепер `border-radius: 0 0 12px 0` — закруглений лише нижній правий кут.

### Текст

Оригінал (українською):

> я — український модороб данделіон, зосереджений на атмосферних механіках, дрібних деталях і захопливому ґеймплеєві

Англійський переклад:

> I am Dandelion, a Ukrainian modder focused on atmospheric mechanics, small details, and captivating gameplay.

Лежить у `js/i18n.js` під ключем `intro`. У `index.html` абзац `.intro` з `data-i18n="intro"`. За замовчуванням показується англійська, перемикач міняє мову.

`.page-body` займає місце під хедером і центрує текст по горизонталі й вертикалі.

### Шрифт

Замість DotGothic16 / Pixelify Sans підключено `family=Tiny5`. Tiny5 має кирилицю, тому запасний піксельний шрифт більше не потрібен.

---

## 3. Що варто знати перед наступними змінами

1. Новий текст — тільки через `i18n.js` (пара `en` / `ua`) і `data-i18n` в HTML.
2. Український рядок не переписувати «для краси»: лишати як я дав.
3. Верхнє закруглення лівого меню не повертати.

---

## 4. Обмеження

Немає.

---

# Етап 09. Виділення тексту і три стани кульбаби

Дата: 19.08.2026

---

## 1. Що змінено

- Колір виділення тексту: фон `#eebd3b`, літери `#eaf4fc` (майже білі, трохи блакитні)
- Квадрат навколо кульбаби прибрано повністю
- Кнопка має три текстури:
  - звичайний стан — `assets/dandelion.png`
  - наведення — `assets/dandelion_puff.png`
  - затиснута ліва кнопка миші — `assets/golden_dandelion.png`

---

## 2. Як це зроблено

### Виділення

У `css/style.css` стоять `::selection` і `::-moz-selection`.

### Кнопка

`.menu-btn` більше не має розміру-квадрата, рамки і фону `#96a5d5`. Лишилась прозора кнопка з трьома картинками.

Стани через CSS:

- `:hover` показує `.icon-hover`
- `:active` показує `.icon-press` і ховає інші — тільки поки затиснута ЛКМ

Золота кульбаба вже була в `assets/golden_dandelion.png`.

Масштаб `scale(1.8)` у контейнері 48px поки лишив, щоб поточні PNG виглядали як раніше. Коли заміню текстури на однакові за розміром — цей зум можна прибрати.

---

## 3. Що варто знати перед наступними змінами

1. Нові три кадри кнопки класти замість `dandelion.png`, `dandelion_puff.png`, `golden_dandelion.png`.
2. Якщо нові файли вже одного розміру і без зайвого поля — прибрати `transform: scale(1.8)` на `.menu-btn-icon img`.
3. Золотий кадр не лишається після кліку, лише на час затискання.

---

## 4. Обмеження

Поточні три PNG ще різної «висоти» кадру; після заміни на однакові це зникне.

---

# Етап 10. Нові текстури кнопки і меню зліва

Дата: 19.08.2026

---

## 1. Що змінено

- Кнопка-кульбаба бере нові файли з `assets/`:
  - звичайний стан — `dandelion_button.png`
  - наведення — `dandelion_button_hovered.png`
  - затиснута ЛКМ — `dandelion_button_pressed.png`
- Зум `scale(1.8)` прибрано: усі три текстури одного розміру, показується кадр цілком
- Меню кульбаби виїжджає зліва з-за краю екрана
- У меню чотири сірі кола по горизонталі — заготовки під майбутні кнопки

---

## 2. Як це зроблено

### Кнопка

Шляхи в `index.html` замінені на нові PNG. `.menu-btn-icon` 64×64, `object-fit: contain`, без обрізання і без збільшення.

### Меню

Закрите: `transform: translateX(-100%)` — панель стоїть ліворуч за екраном.

Відкрите: `translateX(0)` за 0.25s.

`overflow-x: hidden` на `html, body`, щоб виїзд не робив горизонтальний скрол.

Усередині чотири `.side-slot` — круглі кнопки 36px, колір `#8a8a8a`, без іконок і підписів.

Анімація мови лишилась окремо (з’явлення вниз), щоб виїзд кульбаби був саме зліва, а не через прозорість.

---

## 3. Що варто знати перед наступними змінами

1. Іконки в кола ставити всередину `.side-slot`, текст не вигадувати.
2. Старі `dandelion.png`, `dandelion_puff.png`, `golden_dandelion.png` на кнопці більше не використовуються.
3. Розмір кнопки на екрані зараз 64px; якщо треба інший — міняти тільки `.menu-btn-icon`.

---

## 4. Обмеження

Кола поки нічого не роблять.

---

# Етап 11. Меню з кнопки і іконка мови

Дата: 19.08.2026

---

## 1. Що змінено

- Меню кульбаби більше не має фону, рамки і закруглень — лишились тільки чотири кола і анімація
- Кола виїжджають ніби з кнопки кульбаби (з-під неї вправо)
- Чорне коло мови замінено на `assets/language_button.png`

---

## 2. Як це зроблено

### Меню

Кнопка і меню стоять у `.menu-wrap` (ширина під кнопку + чотири кола). `overflow: hidden`, щоб у закритому стані кола не виглядали зліва.

`.side-menu` без фону і без паддінгу панелі. Стоїть одразу праворуч від кнопки (`left: 64px`).

- закрите: `translate(-100%, -50%)` — ховається під кнопкою
- відкрите: `translate(0, -50%)` — виїжджає вправо

Кнопка має більший `z-index`, тож кола виїжджають з-під неї.

### Мова

`.lang-dot` прибрано. У `.lang-toggle` стоїть `language_button.png`, розмір 64×64 як у кульбаби. Hover / press для мови не додавав — є лише один файл.

---

## 3. Що варто знати перед наступними змінами

1. Якщо з’являться hover/press для мови — ставити так само, як на кульбабі.
2. Фон панелі меню не повертати, поки я сам не попрошу.
3. Іконки в `.side-slot` додавати всередину кіл.

---

## 4. Обмеження

Для мови поки один кадр, без наведення і натискання.

---

# Етап 12. Іконки соцмереж, перегляди і вподобайки

Дата: 19.08.2026

---

## 1. Що змінено

- Сірі кола в меню кульбаби тимчасово замінені на іконки Discord, Telegram, Modrinth, Reddit. Посилань немає — я їх не давав.
- На головній є кнопка (сердечко + око, без вигаданого напису), веде на `post.html`
- `post.html` порожня: тільки сердечко, око і два лічильники
- Сердечко зафарбовується золотим `#ffd700` при вподобайці; повторний клік знімає заливку і −1
- Захід на цю сторінку дає +1 перегляд, один раз на людину
- Головна і інші сторінки без лічильника перегляди не рахують

---

## 2. Як це зроблено

### Іконки

`assets/icons/discord.svg`, `telegram.svg`, `modrinth.svg`, `reddit.svg`.

`.side-slot` більше не сіре коло, лише картинка 36×36.

### Лічильники

- `js/stats.js` — клієнт
- `server.py` — маленький сервер на стандартному Python
- `data/stats.json` — цифри і списки хто вже дивився / лайкнув

Сторінка з лічильником має `data-page-id` (зараз `post`). Далі під кожним постом буде свій id.

Людина поки = id у `localStorage` (`dandelion-visitor`) і cookie. Це не акаунт: інший браузер або очищення даних = нова людина.

Якщо `server.py` запущений, цифри спільні для всіх, хто відкриває сайт через нього.

Якщо відкрити файл напряму — сервера немає, тоді лічильник живе тільки в цьому браузері (щоб механіку все одно було видно).

Запуск:

```
python server.py
```

Далі: http://127.0.0.1:8080/

### SQL

SQL зараз не стоїть і не потрібен. Файл JSON вистачає для кількох сторінок.

SQL варто буде додати пізніше, коли з’являться багато постів, коментарі й акаунти.

---

## 3. Що варто знати перед наступними змінами

1. Посилання на Discord / Telegram / Modrinth / Reddit ставити тільки коли я їх дам.
2. Нова сторінка з лічильником: свій HTML + `data-page-id` + підключити `js/stats.js`.
3. Напис на кнопці з головної я ще не давав — тому там лише іконки.

---

## 4. Обмеження

- Без сервера цифри не спільні між людьми.
- Один перегляд / лайк прив’язані до браузера, не до справжнього акаунта.
- Коментарів немає.

---

# Етап 13. GitHub Pages

Дата: 19.08.2026

---

## 1. Що змінено

- Створено публічний репозиторій https://github.com/Norimaru13/dandelionua
- Код сайту залитий (HTML, CSS, JS, SVG, workflow)
- PNG через API не проходять як картинки — їх треба дослати одним `git push` з цього комп’ютера
- Додано `.nojekyll` і `.github/workflows/pages.yml`
- Встановлено Git
- Actions падає з `Resource not accessible by integration`: токен не може **створити** Pages. Треба один раз увімкнути Pages у Settings.

---

## 2. Як це зроблено

Акаунт GitHub: **Norimaru13**.

Адреса сайту після деплою:

https://norimaru13.github.io/dandelionua/

Actions зараз падає, бо Pages ще не ввімкнений. Один раз відкрий:

https://github.com/Norimaru13/dandelionua/settings/pages

1. **Source** → **GitHub Actions**
2. Збережи
3. Actions → останній червоний запуск → **Re-run jobs**

Після цього сайт має відкритися. Картинок ще не буде, поки не зробиш `git push` з PNG.

Локальний коміт уже є. Щоб дослати картинки, у PowerShell:

```
cd $env:USERPROFILE\Desktop\dandelionua
& "C:\Program Files\Git\cmd\git.exe" push -u origin main
```

GitHub попросить увійти. Після пушу Actions збере сайт.

На GitHub Pages немає `server.py`, тому лічильники там будуть тільки в браузері відвідувача (localStorage), не спільні для всіх.

---

## 3. Що варто знати перед наступними змінами

1. Після першого успішного `git push` наступні оновлення — знову `git add`, `git commit`, `git push`.
2. Спільні лайки/перегляди на Pages самі не запрацюють — потрібен окремий хостинг API.

---

## 4. Обмеження

- Без `git push` від мене PNG на Pages ще немає.
- Лічильники на Pages не глобальні.

---

# Етап 14. Картинки на Pages і Supabase

Дата: 19.08.2026

---

## 1. Що змінено

- Текстури для GitHub Pages кладуться як hex-файли в `assets/embed/hex/`. Сторінка сама збирає з них PNG.
- Лічильники підключені до **Supabase** (не Firebase): так простіше на статичному Pages.
- Поки в `js/config.js` порожньо — цифри лишаються локальними, доки я не вставлю URL і anon key.

---

## 2. Як увімкнути спільні лайки

1. Зайди на https://supabase.com і створи акаунт.
2. **New project** — назва будь-яка, регіон ближчий, пароль бази запиши собі.
3. Почекай, поки проєкт стане зеленим.
4. Ліворуч **SQL Editor** → **New query**.
5. Встав увесь файл `docs/supabase.sql` і натисни **Run**.
6. Ліворуч **Project Settings** → **API**.
7. Скопіюй **Project URL** і **anon public** ключ (не service_role).
8. Надішли мені ці два рядки — я впишу їх у `js/config.js` і залию на сайт.

---

## 3. Що варто знати

1. `anon` ключ можна тримати на сайті, якщо SQL уже з RLS, як у `docs/supabase.sql`.
2. `service_role` на сайт не класти.
3. Firebase не ставив: для лічильників вистачає Supabase.

---

## 4. Обмеження

На момент етапу 14 спільні цифри ще чекали ключі. Підключення і перевірка — у `STAGE_15_SUPABASE_LIVE.md`.

---

# Етап 15. Ключі Supabase і живі текстури

Дата: 19.08.2026

---

## 1. Що змінено

- У `js/config.js` стоїть **Project URL** і **publishable** ключ. Секретний ключ на сайт не клав.
- Лічильники на `post.html` ходять у Supabase (`record_view`, `toggle_like`). Якщо виклик не вийде — лишається запасний варіант через localStorage.
- Справжні PNG уже в репозиторії і на GitHub Pages (кнопка, хедер, лого, мова).

---

## 2. Як саме

- З посилання прибрав `/rest/v1/`. У клієнті потрібен корінь проєкту: `https://bohgupycmdvosondvbyl.supabase.co`.
- На сайт пішов лише `sb_publishable_...`. `sb_secret_...` у файлах немає і не має з’являтися.
- SQL з `docs/supabase.sql` уже виконаний: таблиці `page_stats` / `page_visitors` і RPC відповідають.

---

## 3. Де що лежить

- `js/config.js` — URL і publishable ключ
- `js/stats.js` — виклики RPC і запасний localStorage
- `docs/supabase.sql` — таблиці, RLS, функції
- Живий сайт: https://norimaru13.github.io/dandelionua/
- Сторінка лічильників: https://norimaru13.github.io/dandelionua/post.html

---

## 4. Що варто знати

1. Секретний ключ, який я скинув у чат, треба **видалити** в Supabase: Project Settings → API Keys → створити новий secret, старий видалити. Сайт від цього не зламається: він секрет не використовує.
2. Publishable ключ на статичному сайті — нормально, бо доступ іде тільки через RLS і RPC.
3. Один перегляд і один лайк на людину рахуються по `localStorage` id відвідувача.
4. Текстури на Pages: якщо браузер показує стару версію — Ctrl+F5.

---

## 5. Обмеження

- Іконки Discord / Telegram / Modrinth / Reddit досі без посилань.
- GitHub Pages не запускає `server.py`. Спільні цифри йдуть лише через Supabase.
- Секретний ключ після витоку в чат треба замінити, навіть якщо він ніде в репозиторії не лежить.

---

# Етап 16. Чотири стани кнопок, сховане меню, без тестової сторінки

Дата: 19.08.2026

---

## 1. Що змінено

- Прибрано тестову сторінку з вподобайками: `post.html`, посилання з головної, `js/stats.js`, стилі лайків.
- Кнопка-кульбаба має четвертий стан: `assets/dandelion_button_active.png`, коли меню відкрите.
- Кнопка мови теж має чотири стани: `language_button.png` / `_hovered` / `_pressed` / `_active`.
- Іконки в меню кульбаби більше не сидять під кнопкою. Поки меню закрите — їх не видно взагалі.
- У меню додано MCreator і CurseForge.

---

## 2. Як саме

### Стани кнопок

Обидві кнопки мають клас `tex-btn` і чотири картинки в `.btn-tex`:

- звичайна — `.icon-off`
- наведення — `.icon-hover`
- затискання ЛКМ — `.icon-press`
- меню відкрите — `.icon-active`

Пріоритет: press → hover → active → default. `is-open` вішається і на кнопку, і на меню (`js/main.js`).

### Меню

Іконки стоять у `.side-menu-clip` з `overflow: hidden` одразу праворуч від кнопки, не під нею.

Закрите меню: `translateX(-100%)`, `opacity: 0`, `visibility: hidden`. Іконки обрізаються кліпом і не просвічують крізь прозору PNG кнопки.

Відкрите: виїжджають з правого краю кнопки.

### Іконки

`assets/icons/mcreator.svg`, `assets/icons/curseforge.svg` — так само без посилань, як і решта.

---

## 3. Що варто знати

1. Посилання на соцмережі ставити тільки коли я їх дам.
2. `js/config.js` і `docs/supabase.sql` не видаляв — знадобляться, якщо знову будуть спільні лічильники на реальних сторінках.
3. Нові стани кнопок — файли `*_active.png` у `assets/`.

---

## 4. Обмеження

- Шість іконок без посилань.
- На вузькому екрані рядок іконок може не вміститися поруч із хедером.

---

# Етап 17. Порядок іконок і посилання Modrinth

Дата: 19.08.2026

---

## 1. Що змінено

- Порядок у меню кульбаби, зліва направо: Modrinth, CurseForge, MCreator, Telegram, Discord, Reddit.
- Modrinth має посилання: https://modrinth.com/user/dandelion_ua

---

## 2. Як саме

У `index.html` іконки переставлені. Modrinth — це `<a class="side-slot">` з `target="_blank"` і `rel="noopener noreferrer"`. Решта лишились кнопками без href.

У `.side-slot` додано `display: block` і `text-decoration: none`, щоб якір виглядав так само, як кнопка.

---

## 3. Що варто знати

Посилання на CurseForge, MCreator, Telegram, Discord і Reddit ставити тільки коли я їх дам. Тоді замінити відповідну кнопку на такий самий `<a class="side-slot">`.

---

## 4. Обмеження

П’ять іконок досі без адрес.

---

# Етап 18. Усі іконки в `assets/icons/`

Дата: 19.08.2026

---

## 1. Що змінено

- Усі PNG (кнопки, хедер, лого, мова, старі текстури) перенесені в `assets/icons/`.
- У меню Discord тепер `assets/icons/discord_link.png`, а не старий SVG.
- Шляхи в `index.html` оновлені під нову папку.

---

## 2. Як саме

Кожна картинка на сторінці береться з `assets/icons/...`. Favicon теж.

`_build_embed.py` читає PNG з тієї ж папки, якщо знову збирати hex.

`.side-slot img` має `image-rendering: pixelated`, щоб піксельний Discord не милився.

---

## 3. Що варто знати

1. Нові текстури класти в `assets/icons/`.
2. Посилання Discord ще немає — тільки картинка.
3. Старі SVG (Discord / Telegram / Modrinth / Reddit / MCreator / CurseForge) лишились у тій самій папці. На сайті з них ідуть усі, крім Discord.

---

## 4. Обмеження

Discord без URL, поки я його не дам.

---

# Етап 19. Текст і таблиця версій на головній

Дата: 19.08.2026

---

## 1. Що змінено

- На головній два абзаци про автора (український оригінал + англійський переклад).
- Між абзацами невеликий інтервал.
- Після тексту таблиця «Підтримувані версії:» / «Supported versions:».
- Колонки: fabric, neoforge, forge, datapacks.
- Рядки: 26.2, 26.1.2, 26.1.1, 26.1, 1.21.8, 1.21.1, 1.21, 1.20.1, 1.20, 1.19.4, 1.18.2.
- Підтримується — `yes_icon.png`, ні — `no_icon.png`.

---

## 2. Як саме

Тексти в `js/i18n.js`: `intro`, `intro2`, `versions_title`.

Таблиця в `index.html`. Підтримка:

- fabric: 26.2, 26.1.2
- neoforge: 26.2, 26.1.2, 1.21.8, 1.21.1
- forge: 1.20.1
- datapacks: усі рядки таблиці

`.home-copy` тримає абзаци з `gap: 12px`. Тіло сторінки більше не центрує блок по вертикалі — контенту стало більше, скрол іде зверху.

---

## 3. Що варто знати

1. Назви колонок лишив як я дав: fabric, neoforge, forge, datapacks.
2. Нові галочки / хрестики міняти в таблиці в `index.html`.
3. Іконки лежать у `assets/icons/`.

---

## 4. Обмеження

Оформлення таблиці мінімальне — кольорів і рамок я окремо не задавав.

---

# Етап 20. Оформлення таблиці і відступи body

Дата: 19.08.2026

---

## 1. Що змінено

- Інтервал між першим і другим абзацами більший у півтора раза (було 12px, стало 18px).
- Таблиця в рамці з закругленими кутами, колір рамки й внутрішніх ліній `#96a5d5`.
- Зовнішня рамка вдвічі товща за внутрішні лінії (2px / 1px).
- Верхні рядки і лівий стовпець залиті `#6371af`, інші клітинки `#8491bd`.
- У body по краях відступи ≈ 1/5 ширини екрана (`20vw`), колір смуг `#578c67`. Середина лишилась `#67a77a`.

---

## 2. Як саме

`.home-copy` — `gap: 18px`.

`.versions` — `border-collapse: separate`, зовні `2px solid #96a5d5`, `border-radius: 8px` як у меню мови. Клітинки мають тільки правий і нижній бордер `1px`, щоб лінії не подвоювались.

`body` фон `#578c67`. `.page-body` має `margin: 0 20vw` і фон `#67a77a`. Хедер і смужки на всю ширину.

---

## 3. Що варто знати

1. Верх таблиці — і рядок «Підтримувані версії:», і рядок fabric / neoforge / forge / datapacks.
2. На вузькому екрані бічні 20vw з’їдають багато місця; таблиця може гортатися вбік.

---

## 4. Обмеження

Радіус кутів таблиці я не задавав окремо — взяв 8px, як уже є на меню мови.

---

# Етап 21. Кольори рамки, Forge 1.19.4, іконка CurseForge

Дата: 19.08.2026

---

## 1. Що змінено

- Зовнішня рамка таблиці `#e29020`, внутрішні лінії `#c46002`.
- Forge 1.19.4 тепер з галочкою.
- Колонка `datapacks` перейменована на `data packs`.
- У меню CurseForge стоїть `assets/icons/curseforge_link.png`.
- Для тесту колір тексту зверху в трьох комірках: fabric `#eebd3b`, neoforge `#e29020`, forge `#c46002`.

---

## 2. Як саме

Класи `.text-a` / `.text-b` / `.text-c` на трьох заголовках колонок. Решта тексту таблиці без цих кольорів.

---

## 3. Що варто знати

Тестові кольори тексту прибрати або залишити один, коли я скажу який підходить.

---

## 4. Обмеження

Посилання CurseForge ще немає.

---

# Етап 22. Заливка таблиці, фіксований хедер, скролбар

Дата: 19.08.2026

---

## 1. Що змінено

- Лінії таблиці знову блакитні `#96a5d5`.
- Заливка внутрішніх комірок `#e29020`, верхніх і лівого стовпця `#c46002`.
- Колір тексту в таблиці як був (без тестових кольорів).
- Три тестові слова над таблицею: `eebd3b`, `e29020`, `c46002` у відповідних кольорах.
- В українській колонка data packs = «пакети даних».
- Хедер зі смужками не їде зі скролом.
- Скролбар — тонка смужка `#c46002`, лише під хедером, на хедер не залазить.

---

## 2. Як саме

`.site-top` тримає хедер і смужки. `.site-scroll` скролиться (`overflow-y: auto`). Сторінка (`html, body`) без віконного скролу.

Смуга: `scrollbar-width: thin`, webkit ширина 8px, без стрілок, thumb `#c46002`, track `#578c67`.

`col_datapacks` у `js/i18n.js`.

---

## 3. Що варто знати

Тестові три слова прибрати, коли оберу колір тексту.

---

## 4. Обмеження

Кастомний скролбар у Firefox тонший за webkit; колір той самий.

---

# Етап 23. Інверсія заливки і колір тексту

Дата: 19.08.2026

---

## 1. Що змінено

- Заливку комірок поміняв місцями: всередині `#c46002`, зверху і зліва `#e29020`.
- Три тестові слова прибрано.
- Основний текст на сторінці (абзаци) `#893500`. Текст таблиці лишився чорним.

---

## 2. Як саме

`.page-body { color: #893500; }`, у `.versions` окремо `color: #000`.

---

## 3. Що варто знати

Нові абзаци поза таблицею автоматично візьмуть `#893500`.

---

## 4. Обмеження

Колір тексту в таблиці я не міняв.

---

# Етап 24. Зелена палітра тексту і таблиці

Дата: 19.08.2026

---

## 1. Що змінено

- Основний текст `#28402f`.
- Заливка всередині таблиці `#375942`.
- Заливка зверху і зліва `#77bf8c`.

---

## 2. Як саме

Кольори в `css/style.css`. Щоб браузер не тримав старий файл, у `index.html` стоїть `style.css?v=24`.

---

## 3. Що варто знати

Текст таблиці лишається чорним.

---

## 4. Обмеження

Немає.

---

# Етап 25. Згортання таблиці і колір тексту в ній

Дата: 19.08.2026

---

## 1. Що змінено

- Текст у таблиці `#28402f`.
- Заливку й лінії таблиці не чіпав — лишив як я вже поставив у файлі.
- Комірка «Підтримувані версії:» / «Supported versions:» стала кнопкою.
- Таблиця завжди стартує згорнутою. Клік по цьому рядку розгортає й згортає решту.

---

## 2. Як саме

Кнопка `data-versions-toggle` у першому рядку. Клас `is-open` на `table.versions`. Без нього сховані рядок колонок і `tbody`.

Логіка в `js/main.js`.

---

## 3. Що варто знати

Згорнутий стан — лише одна смужка з написом у тій самій рамці.

---

## 4. Обмеження

Немає окремої іконки стрілки — я її не давав.

---

# Етап 26. Сайдбари, ширший другий абзац, таблиця

Дата: 19.08.2026

---

## 1. Що змінено

- Другий абзац ширший за перший (`42em` проти `28em`).
- Кути таблиці гострі (`border-radius: 0`).
- Текст кнопки таблиці по центру.
- Зелені смуги з боків — окремі сайдбари `.sidebar` на `20vw`. На екрані вужчому за 720px їх немає.
- Скролбар лежить на правому сайдбарі: місце під смугу завжди є (`overflow-y: scroll`, правий сайдбар `20vw − 8px`), тому середина не стрибає.

---

## 2. Як саме

`.site-row` — flex: лівий сайдбар, `main.page-body`, правий сайдбар.

Трек скролбара `#578c67`, як сайдбар. Порожня смуга зливається з правим краєм.

---

## 3. Що варто знати

1. Ширину другого абзаца можна ще підкрутити.
2. Поріг «телефон» — 720px.
3. У Firefox тонка смуга може бути не рівно 8px — тоді правий край може відрізнятися на 1–2 пікселі.

---

## 4. Обмеження

Сайдбари порожні. На вузькому екрані скролбар уже на краю контенту, бо бокових смуг немає.

---

# Етап 27. Скролбар, resource packs, мова, футер

Дата: 19.08.2026

---

## 1. Що змінено

- Скролбар квадратний, утричі ширший (24px). Повзунок `#28402f`, фон `#375942`.
- У таблицю додано стовпець resource packs / пакети ресурсів. Галочок я не давав — клітинки порожні.
- Таблиця тягнеться до сайдбарів.
- Меню мови: та сама механіка, що в кульбаби, але вліво. Фону немає. Поки два прямокутники: червоний EN, синій UA.
- Футер не фіксований, колір хедера `#6371af`. Текст: «сайт створено Норімару, за ідеї Данделіону» / «site created by Norimaru, based on Dandelion's idea».

---

## 2. Як саме

Правий сайдбар тепер `20vw − 24px`, щоб широка смуга лишалась на ньому.

`.lang-menu-clip` стоїть абсолютом ліворуч від кнопки мови. Меню `translateX(100%)` → `translateX(0)`.

Футер у `.site-stack` під `.site-row`, скролиться разом зі сторінкою. Якщо контенту мало — лишається внизу екрана.

---

## 3. Що варто знати

1. Для resource packs треба список версій з галочками / хрестиками.
2. Прапори замінити, коли будуть картинки.
3. У Firefox ширину скролбара браузер задає сам (`auto`), рівно 24px гарантовано в Chrome / Edge.

---

## 4. Обмеження

Клітинки resource packs порожні.

---

# Етап 28. Великий скролбар і звук кліку

Дата: 19.08.2026

---

## 1. Що змінено

- Скролбар ще вчетверо ширший: було 24px, стало 96px. Квадратний, кольори ті самі.
- Правий сайдбар `20vw − 96px`, щоб смуга лишалась на ньому.
- Клік по кнопках (і по слотах меню кульбаби) грає `assets/minecraft_click.mp3`.

---

## 2. Як саме

У `js/main.js` слухач `click` на `button` і `a.side-slot`. Кожен клік створює новий `Audio`, щоб звуки могли накладатися.

---

## 3. Що варто знати

Браузер може заблокувати звук, поки користувач нічого не натиснув на сторінці — перший клік якраз це й робить.

---

## 4. Обмеження

96px на вузькому екрані з’їдає багато сайдбару. На телефоні сайдбарів немає, смуга все одно широка. Firefox ширину 96px може не дати.

---

# Етап 29. Власна форма скролбара

Дата: 19.08.2026

---

## 1. Що змінено

- Скролбар більше не стандартний Chrome з іншим кольором.
- Фон і повзунок — однакові прямокутники з гострими кутами, 96px.
- Стрілки зверху і знизу прибрані.

---

## 2. Як саме

`scrollbar-color` у Chrome лишав системну форму. Прибрав його з `.site-scroll`, лишив лише `::-webkit-scrollbar*`.

Для Firefox, де webkit-псевдокласів немає, колір лишається через `@supports not selector(::-webkit-scrollbar)`.

---

## 3. Що варто знати

У Firefox форму все одно малює браузер — там кастомна геометрія недоступна.

---

## 4. Обмеження

Форма 96px гарантована в Chrome / Edge.

---

# Етап 30. Свій прямокутний скролбар без фону

Дата: 19.08.2026

---

## 1. Що змінено

- Системний скролбар Chrome схований.
- Замість нього — свій повзунок: прямокутник 96px, гострі кути, колір `#28402f`.
- Фону / треку немає. Стрілок немає.

---

## 2. Як саме

Chrome не дає нормально змінити форму системної смуги. Тому `.site-scroll` ховає нативний скрол, а `.scroll-thumb` малюється поверх правого краю.

JS у `main.js` рухає повзунок і дає перетягування.

---

## 3. Що варто знати

Правий сайдбар знову на всю `20vw`: місце під системну смугу більше не ріжемо.

---

## 4. Обмеження

Немає.

# Етап 34. Вхід, реєстрація, нік

Дата: 20.08.2026

---

## 1. Що змінено

- Кнопка «увійти» знову клікається: картинка хедера більше не перехоплює натискання. Саму кнопку посунув лівіше від мови.
- У ніку можна `-` і `.`
- SQL реєстрації виправлений: `crypt` береться з `extensions` (Supabase). Без цього `account_register` узагалі не створювалась.

---

## 2. Як саме

`.header-title { pointer-events: none; }`

У `docs/accounts.sql` хеш пароля: `extensions.crypt` / `extensions.gen_salt`.

---

## 3. Що варто знати

Треба ще раз натиснути **Run** для всього `docs/accounts.sql` у SQL Editor. Інакше створення профілю далі падає.

---

## 4. Обмеження

Поки SQL не перезапущений — реєстрація не працює.

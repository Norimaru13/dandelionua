# Інструкція проєкту: Сайт модів Dandelion_ua

Я (власник проєкту) створюю сайт для модів Minecraft, які робить мій друг під ніком **Dandelion_ua**.

Розробка ведеться **поступово**. Спочатку тримаємо мінімальну базу, потім додаємо лише те, що я прошу.

---

## 1. Мета проєкту

Створити сайт, на якому можна:
- Переглядати список модів Dandelion_ua
- Читати описи, дивитися скріншоти
- Завантажувати моди

Це довгострокова мета. Зараз сайт ще не має цих розділів.

---

## 2. Правила роботи зі мною (Grok)

- Інструкція і файли етапів лежать у папці `docs/`. У корені сайту їх немає.
- Після **кожного завершеного етапу** ти створюєш окремий текстовий файл (наприклад `STAGE_31_....md`) у папці `docs/`.
- У цьому файлі детально описуєш:
  - Що саме було додано
  - Як саме це зроблено (структура файлів, ключові рішення, які технології/підходи використані)
  - Де що лежить
  - Які є важливі моменти для майбутніх змін
- Кожні **30 етапів** збираєш їх в один файл (`STAGES_01-30.md`, потім `STAGES_31-60.md` і так далі) і **видаляєш** окремі файли цієї тридцятки. Так менше файлів і зручніше читати.
- Зібране: `docs/STAGES_01-30.md`. Зараз пишемо етапи 31–60 окремо.
- Якщо виникає проблема, помилка або потреба щось змінити — ти спочатку дивишся інструкцію і ці документи, щоб швидко зрозуміти поточний стан і не ламати те, що вже працює.
- Усі відповіді та документацію ти пишеш **від мого обличчя** (від першої особи, як інструкції від мене).
- Кожен етап закінчується робочою версією, яку можна подивитися.

### Контент — головне правило

- **Нічого не додавати від себе.** Не вигадувати тексти, назви, описи, фото, скріншоти, демо-моди, факти про автора.
- На сайті може бути лише те, що я явно дав.
- Я даю матеріал **українською**. Ти перекладаєш його і додаєш **англійською**. Обидві мови мають бути на сайті.
- Якщо чогось немає — лишаємо порожнім. Не заповнюємо «поки що» своїм текстом.

### Дизайн

- Оформлення ставлю **тільки те, що я назвав** (кольори, смужки, картинки, розташування). Нічого не вигадувати зверху.
- Піксельний шрифт: **Tiny5** з Google Fonts (є англійська і українська).
- Виділення тексту: фон `#eebd3b`, літери майже білі з легким блакитним (`#eaf4fc`).
- Майбутній темний Minecraft-вайб окремо не впроваджуємо, якщо я сам цього не попросив.

---

## 3. Мови сайту

- Основна мова за замовчуванням: **англійська**
- Є перемикач на **українську**
- Інтерфейс і контент мають підтримувати обидві мови

---

## 4. Технічний стек

- Чистий HTML + CSS + JavaScript (без важких фреймворків)
- Усе зберігається в папці проєкту
- Профілі: нік + пароль (без пошти). SQL — `docs/accounts.sql`. Лайки лише з профілю.
- `server.py` лишився тільки для локальної перевірки. GitHub Pages його не запускає.

---

## 5. Поточний стан (етап 67)

Тестову `post.html` прибрано. Кнопки кульбаби і мови мають чотири стани. Іконки меню з’являються тільки коли меню відкрите.

Репозиторій: https://github.com/Norimaru13/dandelionua  
Сайт: https://norimaru13.github.io/dandelionua/

На сайті зараз є:
- Хедер фіксований (разом зі смужками), скрол тільки під ним
- Фон сторінки `#67a77a`; зліва і справа сайдбари `#578c67` шириною ≈ 1/5 екрана (на телефоні ховаються)
- Три смужки між хедером і тілом, зверху вниз: `#96a5d5`, `#0057b8`, `#ffd700`
- Кнопка-кульбаба: default / hover / press / active (`dandelion_button_active.png`, коли меню відкрите)
- Меню кульбаби, зліва направо: Modrinth, CurseForge, MCreator, Telegram, Discord, Reddit
- Посилання (нова вкладка): Modrinth https://modrinth.com/user/dandelion_ua ; CurseForge https://www.curseforge.com/members/dandelion_ua/projects ; MCreator https://mcreator.net/user/800904/dandelionua ; Telegram https://t.me/dandelion_ua_tg ; Discord https://discord.gg/SyBSh4hfQ8 ; Reddit https://www.reddit.com/user/kitaro_7/
- Наведення на іконку мережі показує назву мовою сайту
- Іконки меню не видно, поки меню закрите
- Усі картинки лежать у `assets/icons/`
- По центру `assets/icons/dandelion_ua_header.png`
- Основний текст (не таблиця) `#28402f`
- Посередині тіла два абзаци про автора + таблиця підтримуваних версій
- Скролбар головної: бігунок 24px `#28402f`, фон `#375942`. На футер не заходить і нічого його не перекриває. У гуішці створення поста — блакитний `#6371af` без фону (замість браузерного)
- Клік по будь-якій кнопці чи клікабельному елементі грає `assets/minecraft_click.mp3`: меню, посилання, лайки, дизлайки, коментарі (відкрити, надіслати, голос, правки, закрити), вкладки, форма публікації, чіпи фільтрів у формі, підтвердження, профіль. Без звуку лише набір тексту в полях.
- Таблиця згорнута; рядок «Підтримувані версії:» — кнопка, розгортає таблицю
- Таблиця: лінії `#28402f`; заливка всередині `#375942`, зверху і зліва `#578c67`; текст `#28402f`
- Таблиця на всю ширину між сайдбарами
- Таблиця: fabric 26.2 і 26.1.2; neoforge 26.2, 26.1.2, 1.21.8, 1.21.1; forge 1.20.1 і 1.19.4; data packs / пакети даних — усі зі списку; resource packs / пакети ресурсів — клітинки порожні, поки не дам галочки
- Меню мови виїжджає вліво з кнопки, без фону; прапори `en_lang.png` і `uk_lang.png`, розмір 80×61, без обрізання
- Футер не фіксований, колір як хедер `#6371af`. Над ним смужка `#96a5d5`, як між хедером і страйпом. Текст: «© Сайт було створено Норімару за ідеєю Данделіона» / «© The site was created by Norimaru, based on Dandelion's idea»
- Галочка / хрестик: `assets/icons/yes_icon.png`, `assets/icons/no_icon.png`
- Кнопка мови: default / hover / press / active (`language_button_active.png`, коли меню мови відкрите)
- Discord у меню: `assets/icons/discord_link.png`
- Telegram у меню: `assets/icons/telegram_link.png`
- CurseForge у меню: `assets/icons/curseforge_link.png`
- Курсор: `assets/icons/cursor.png`. На кнопках і посиланнях: `assets/icons/cursor_select.png`
- Іконка вкладки: `assets/icons/dandelion_ua_site_logo.png`

- Кнопка «увійти» в хедері. Без профілю сайт дивитися можна, лайк — ні.
- Вподобайка на головній (сторінка `home`)
- Під таблицею вкладки Проєкти / Пости (за замовчуванням Проєкти), по 3 останніх і «дивитись більше». Українською Modrinth — «Модрінт»
- Сторінки `projects.html` і `publications.html` — повний список
- Клік по центральній текстурі хедера веде на головну
- Сірий виступ у хедері зліва — меню сторінок, виїжджає вниз з анімацією
- Клік поза будь-яким меню закриває його
- Кнопка мови лівіше, профіль правіше
- Час поста без секунд. Тип поста: project / publication. SQL: `docs/post_kind.sql`
- Таблиця версій по ширині збігається з постами (відступи 24px)
- Форма: широке вікно, розмитий фон, вкладки EN/UK. Тип у формі: проєкт / Пости. Для поста — назва, короткий опис, текст (скрепка, B/I/U/S і посилання в стилі перемикача мови; можна кілька форматів разом; закреслення вимикається; виділив слово → кнопка ланцюжка → вставив URL, слово лишається текстом і стає підкресленим посиланням; кольори, RGB; квадрат відкриває свою палітру, другий клік закриває). Відступ назва→опис однаковий на EN і UK. Звук кнопок у гуішці не двоїться. Для проєкту — англійська назва, короткий опис, фільтри і прев’ю. SQL: `docs/post_project.sql`. Чернетки: `docs/post_draft.sql` — зберегти бачать лише адміни; хрестик: закрити або зберегти; поруч з опублікувати є зберегти; при правці кнопка «редагувати»
- Дата поста справа. Меню поста — `...`. RGB вертикальний, код `#RRGGBB`; пресети рухають повзунки. Підтвердження плавне
- Фільтри проєкту — прямокутні кнопки, порядок: тип, платформи (Vanilla / Fabric / NeoForge / Forge), версії (26.2 … 1.18, з патчами 1.18–1.21.11), стан, статус. Прев’ю — скрепка. B/I/U в описі. У публікації панель тексту під коміркою, правіше RGB
- Картка проєкту фіксована: прев’ю квадрат 160×160; якщо фото немає — квадрат `#375942`. Справа 2/3 короткий опис (ліміт 300 символів), 1/3 фільтри. Кожна категорія — один блок (не кнопки). Версії: 5 найновіших, решта при наведенні з `+x`. Лоадери: Fabric / NeoForge / Forge, а vanilla / датапак / ресурс-пак при наведенні з `+x`. На головній лише стан, версії і лоадер. Знизу зліва реакції, справа «написати коментар». Пости з тією самою смугою коментарів, без сітки проєкту. Свій коментар можна правити; під часом публікації тоді `(редаговано …)`. SQL: `docs/post_comments.sql`, `docs/post_comment_edit.sql`

Інструкція — `docs/Інструкція.txt`. Етапи 01–30 — `docs/STAGES_01-30.md`. Далі — окремі `STAGE_31_…`.

---

## 6. Структура документації етапів

Кожен файл етапу має містити:
1. Назву етапу і дату
2. Список того, що було додано / змінено
3. Детальний опис реалізації (файли, ключові класи, підходи)
4. Що варто знати для наступних змін
5. Відомі обмеження або тимчасові рішення (якщо є)

---

Цей документ є головною інструкцією. Усі подальші етапи підпорядковуються йому.
При будь-яких змінах у вимогах — спочатку оновлюємо цей файл.
